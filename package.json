{
  "name": "enygma-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd artifacts/enygma && npm install --legacy-peer-deps && npm run build",
    "start": "cd artifacts/enygma && npm start"
  }
}
